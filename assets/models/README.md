# assets/models

3D 모델(.glb) 파일을 넣는 폴더입니다. 여기 넣은 `.glb`는 git에 커밋되고 GitHub Pages로 함께 배포됩니다.

## 올리는 방법 (GitHub 웹에서)

1. GitHub 저장소 페이지에서 브랜치를 고른 뒤 이 폴더(`assets/models`)로 들어갑니다.
2. **Add file → Upload files** 로 `.glb` 파일을 끌어다 놓고 **Commit changes**.

## 파일 이름 규칙

| 대상 | 파일 이름 | 연결 방법 |
|------|-----------|-----------|
| 주인공 | `인하.glb` (`src/player.js`의 `PLAYER_MODEL`) | 파일 이름을 바꾸면 그 상수도 같이 바꿉니다 |
| 몬스터 | 아무 이름이나 가능. 예: `피카츄.glb` | `data/creatures.json`에서 그 몬스터의 `"model"` 에 파일 이름을 적습니다 (`"model": "피카츄.glb"`) |
| 숫자블록 | `nb05_daseot.glb` | (아직 코드 연결 전) |

파일이 없거나 불러오기에 실패하면 자동으로 드래프트 도형이 나오므로 게임이 멈추지 않습니다. 브라우저 콘솔(F12)에 `[models] ... 불러옴` 또는 실패 원인이 찍힙니다.

## 현재 들어 있는 모델

| 파일 | 쓰이는 곳 |
|------|-----------|
| `인하.glb` | 주인공 |
| `이상해씨.glb` | m01 (좋아하는 숫자 1) |
| `파이리.glb` | m02 (2) |
| `피카츄.glb` | m03 (3) |
| `꼬부기.glb` | m05 (5) |
| `디그다.glb` | m04 (4) · 초원 |
| `캐터피.glb` | m06 (6) · 초원 |
| `포니타.glb` | m07 (7) · 동굴, 잡으면 동굴이 밝아짐 |
| `냐옹.glb` | m08 (8) · 동굴 |
| `구구.glb` | m09 (9) · 동굴 |
| `식스테일.glb` | m10 (10) · 얼음 궁전(아직 없음) |
| `이브이.glb` | m11 (12) · 푸른숲 야생. 공격 10·체력 15·5승이면 엄브레온으로 진화 |
| `푸린.glb` | m15 (20) · 밤 탐험(아직 없음) |
| `이상해풀.glb` / `이상해꽃.glb` | m01e / m01ee · 이상해씨의 1·2단계 진화형 (`이상해꽃.glb` 는 푸른숲 보스 b01 에도 쓰임) |
| `리자드.glb` / `리자몽.glb` | m02e / m02ee · 파이리의 1·2단계 진화형 (`리자몽.glb` 는 불의산 보스 b03 에도 쓰임) |
| `라이츄.glb` | m03e · 피카츄의 진화형, 꿈의우주 야생 |
| `메가리자몽.glb` | b05 · 꿈의우주 보스 |
| `잠만보.glb` | m27 · 푸른숲 북서쪽 구석에서 잠 (공격 1, 체력 60) |
| `치코리타.glb` `두두.glb` `파오리.glb` `우츠동.glb` `망키.glb` | 푸른숲 야생 (m28~m32) |
| `탕구리.glb` `질퍽이.glb` `알통몬.glb` `콘팡.glb` `꼬마돌.glb` | 지하동굴 야생 (m33~m36, m18) |
| `셀러.glb` `크랩.glb` `야돈.glb` `독파리.glb` `고라파덕.glb` `발챙이.glb` | 물의길 야생 (m37~m40, m20, m22) |
| `왕눈해.glb` | m40e · 독파리의 진화형 |
| `어니부기.glb` / `거북왕.glb` | m05e / m05ee · 꼬부기의 1·2단계 진화형 (`거북왕.glb` 는 물의길 보스 b04 에도 쓰임) |
| `케이시.glb` `고우스트.glb` `코일.glb` | 꿈의우주 야생 (m24, m26, m41) |
| `엄브레온.glb` | m11e · 이브이의 진화형(고스트), 꿈의우주 야생 |
| `구회.glb` | m43 · 꿈의우주 야생 (페어리, 노란 별) |
| `끼북겅.glb` | m44 · 물의길 모래섬 야생 (물, 아기 거북, 가장 약함) |
| `이상해할로윈.glb` | m45 · 명왕성 야생 (고스트, 호박 이상해씨) |
| `리자풀.glb` `리흑회.glb` `보라거북왕.glb` `거북풀.glb` `거북왕트론.glb` | px01~px05 · 행성 보스: 지구 리자풀 · 태양 리흑회 · 해왕성 보라거북왕 · 금성 거북풀 · 목성 거북왕트론 |
| `이상해불.glb` `이상해물.glb` `토눈.glb` `버빨황.glb` | m46~m49 · 행성 야생: 화성 이상해불 · 천왕성 이상해물 · 지구 토눈 · 토성 버빨황 |
| `가디.glb` / `윈디.glb` | m25 / m25e · 불의산 야생, 가디의 진화형 윈디 |
| `망나뇽.glb` / `메가망나뇽.glb` | px06 토성 보스 / x08 망나뇽을 잡으면 토성에 나타나는 메가급 |
| `에스퍼.glb` | m50 · 명왕성 야생 (마법사 도마뱀, 에스퍼) |
| `손오공.glb` `도토로.glb` `베지터.glb` | UFO 조종사 손오공(꿈의우주·행성 10곳) · 꿀벌집 안내 도토로 · 태양 아레나 앞 라이벌 베지터 |
| `햇님.glb` `보석.glb` `황금빵구.glb` | 줍는 것: 태양 햇님 · 천왕성 보석 · 꿈의우주 황금 별 (`src/main.js` 의 `PICKUP_MODEL`) |
| `포켓몬볼.glb` / `포켓몬큐브.glb` | 대결에서 던지는 몬스터볼(브론즈·실버·골드는 뚜껑 색만 바뀜) / 메가큐브 (`src/battle.js`) |
| `이상해꽃자동차.glb` | 주인공이 타는 이상해꽃 자동차 (`src/car.js`, 앞이 -X 라서 코드에서 90° 돌린다) |
| `뮤.glb` | x07 · 행성 열 곳의 보스를 모두 잡으면 꿈의우주에 나타나는 전설 (체력 320·공격 60, 메가큐브 필요). 걷기·달리기 클립 있음 |
| `나미.glb` `웅이.glb` `봄이.glb` `리리.glb` `코리.glb` `오박사.glb` | 지역 안내 NPC(푸른숲·지하동굴·불의산·물의길·꿈의우주)와 연구소 오박사 (`src/npc.js`) |
| `아이손오공.glb` | 푸른숲 아레나 입구의 대결 코치 NPC "아이 손오공" (`src/world.js`) |

아직 모델이 없어 드래프트 도형으로 나오는 몬스터는 `data/creatures.json` 에 파일 이름이 미리 적혀 있어, 같은 이름의 `.glb` 를 올리기만 하면 바로 적용됩니다:

| 지역 | 파일 이름 |
|------|-----------|
| 보스 | `이상해꽃.glb`(푸른숲) · `롱스톤.glb`(지하동굴) · `리자몽.glb`(불의산) · `거북왕.glb`(물의길) · `메가팬텀.glb`(꿈의우주) |
| 지하동굴 | `주뱃.glb` · `꼬마돌.glb` |
| 불의산 | `마그마.glb` |
| 물의길 | `고라파덕.glb` · `잉어킹.glb` · `발챙이.glb` |
| 꿈의우주 | `삐삐.glb` · `캐이시.glb` · `고오스.glb` |

아직 모델이 없는 종: 주뱃(m17), 롱스톤(b02), 마그마(m19), 삐삐(m23). 드래프트 도형으로 나옵니다. `.glb` 를 올린 뒤 `data/creatures.json` 에서 해당 항목의 `"model": null` 을 `"model": "라이츄.glb"` 처럼 바꾸면 됩니다.

## 용량 줄이기 (로딩 속도)

파일 이름에 괄호나 띄어쓰기는 넣지 않습니다(예: `구회(별).glb` → `구회.glb`, 게임에 보이는 이름은 `data/creatures.json` 의 `name`). 업로드한 원본은 2048px 텍스처 3~4장이 들어 있어 파일당 3~8MB였습니다. 아래 스크립트가 baseColor 1024px, emissive 512px 로 줄이고 normal/metallicRoughness 맵을 빼서 파일당 0.1~1.3MB 로 만듭니다(전체 23MB → 2.3MB). 새 모델을 올린 뒤 한 번 돌려주세요.

```
pip install pillow
python3 tools/shrink_glb.py assets/models/*.glb
```

## 규격

- 높이 약 1m, 원점은 발바닥 중앙, 앞 방향 +Z (지금 들어 있는 모델들과 같은 방향. 뒤를 보고 나오면 3D 툴에서 180° 돌려 다시 내보내기). 크기가 달라도 코드에서 높이 1m로 자동 보정하고, 몬스터별 `scale` 값이 그 위에 곱해집니다.
- 애니메이션(선택): 클립 이름을 `idle`, `walk`로 넣으면 서 있을 때/걸을 때 자동 재생됩니다. 이름이 다르면 첫 번째 클립을 계속 재생합니다.
- 폴리곤 1만 이하 권장(태블릿용). 텍스처는 .glb 안에 포함(embedded)해서 내보내세요.
- 자세한 내용은 `docs/06_dev_plan_and_assets.md` 참고.
