# assets/models

3D 모델(.glb) 파일을 넣는 폴더입니다. 여기 넣은 `.glb`는 git에 커밋되고 GitHub Pages로 함께 배포됩니다.

## 올리는 방법 (GitHub 웹에서)

1. GitHub 저장소 페이지에서 브랜치를 고른 뒤 이 폴더(`assets/models`)로 들어갑니다.
2. **Add file → Upload files** 로 `.glb` 파일을 끌어다 놓고 **Commit changes**.

## 파일 이름 규칙

| 대상 | 파일 이름 | 연결 방법 |
|------|-----------|-----------|
| 주인공 | `인하.glb` (`src/player.js`의 `PLAYER_MODEL`) | 파일 이름을 바꾸면 그 상수도 같이 바꿉니다 |
| 몬스터 | 아무 이름이나 가능. 예: `피카츄.glb` | `data/creatures.json`에서 그 몬스터의 `"model"` 에 파일 이름을 적습니다 (`"model": "피카츄.glb"`) |
| 숫자블록 | `nb05_daseot.glb` | (아직 코드 연결 전) |

파일이 없거나 불러오기에 실패하면 자동으로 드래프트 도형이 나오므로 게임이 멈추지 않습니다. 브라우저 콘솔(F12)에 `[models] ... 불러옴` 또는 실패 원인이 찍힙니다.

## 현재 들어 있는 모델

| 파일 | 쓰이는 곳 |
|------|-----------|
| `인하.glb` | 주인공 |
| `이상해씨.glb` | m01 (좋아하는 숫자 1) |
| `파이리.glb` | m02 (2) |
| `피카츄.glb` | m03 (3) |
| `꼬부기.glb` | m05 (5) |
| `디그다.glb` | m04 (4) · 초원 |
| `캐터피.glb` | m06 (6) · 초원 |
| `포니타.glb` | m07 (7) · 동굴, 잡으면 동굴이 밝아짐 |
| `냐옹.glb` | m08 (8) · 동굴 |
| `구구.glb` | m09 (9) · 동굴 |
| `식스테일.glb` | m10 (10) · 얼음 궁전(아직 없음) |
| `이브이.glb` | m11 (12) · 얼음 궁전(아직 없음) |
| `푸린.glb` | m15 (20) · 밤 탐험(아직 없음) |

아직 모델이 없어 드래프트 도형으로 나오는 몬스터는 `data/creatures.json` 에 파일 이름이 미리 적혀 있어, 같은 이름의 `.glb` 를 올리기만 하면 바로 적용됩니다:

| 지역 | 파일 이름 |
|------|-----------|
| 보스 | `이상해꽃.glb`(푸른숲) · `롱스톤.glb`(지하동굴) · `리자몽.glb`(불의산) · `거북왕.glb`(물의길) · `뮤.glb`(꿈의우주) |
| 지하동굴 | `주뱃.glb` · `꼬마돌.glb` |
| 불의산 | `마그마.glb` · `가디.glb` |
| 물의길 | `고라파덕.glb` · `잉어킹.glb` · `발챙이.glb` |
| 꿈의우주 | `삐삐.glb` · `캐이시.glb` · `고오스.glb` |

진화형(라이츄 m03e, 리자드 m02e, 이상해풀 m01e, 어니부기 m05e)은 아직 모델이 없어 드래프트 도형으로 나옵니다. `.glb` 를 올린 뒤 `data/creatures.json` 에서 해당 항목의 `"model": null` 을 `"model": "라이츄.glb"` 처럼 바꾸면 됩니다.

## 용량 줄이기 (로딩 속도)

업로드한 원본은 2048px 텍스처 3~4장이 들어 있어 파일당 3~5MB였습니다. 아래 스크립트가 baseColor 1024px, emissive 512px 로 줄이고 normal/metallicRoughness 맵을 빼서 파일당 0.1~1.3MB 로 만듭니다(전체 23MB → 2.3MB). 새 모델을 올린 뒤 한 번 돌려주세요.

```
pip install pillow
python3 tools/shrink_glb.py assets/models/*.glb
```

## 규격

- 높이 약 1m, 원점은 발바닥 중앙, 앞 방향 +Z (지금 들어 있는 모델들과 같은 방향. 뒤를 보고 나오면 3D 툴에서 180° 돌려 다시 내보내기). 크기가 달라도 코드에서 높이 1m로 자동 보정하고, 몬스터별 `scale` 값이 그 위에 곱해집니다.
- 애니메이션(선택): 클립 이름을 `idle`, `walk`로 넣으면 서 있을 때/걸을 때 자동 재생됩니다. 이름이 다르면 첫 번째 클립을 계속 재생합니다.
- 폴리곤 1만 이하 권장(태블릿용). 텍스처는 .glb 안에 포함(embedded)해서 내보내세요.
- 자세한 내용은 `docs/06_dev_plan_and_assets.md` 참고.
